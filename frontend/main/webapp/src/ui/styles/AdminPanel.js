const AdminPanel = (theme) => ({
  switchGrid: {
    display: "flex",
    alignItems: "center",
  },
  userIdTypo: {
    textTransform: "none",
  },
  filterContainer: {
    marginBottom: "20px",
  },
});

export default AdminPanel;
