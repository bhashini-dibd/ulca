/**
 *
 * LoginSocialInstagram
 *
 */
import React from 'react';
import { objectType, IResolveParams } from '../';
interface Props {
    scope?: string;
    state?: string;
    client_id: string;
    className?: string;
    redirect_uri: string;
    response_type?: string;
    children?: React.ReactNode;
    onReject: (reject: string | objectType) => void;
    onResolve: ({ provider, data }: IResolveParams) => void;
}
export declare const LoginSocialInstagram: React.MemoExoticComponent<({ state, client_id, className, redirect_uri, scope, response_type, children, onResolve }: Props) => JSX.Element>;
export default LoginSocialInstagram;
