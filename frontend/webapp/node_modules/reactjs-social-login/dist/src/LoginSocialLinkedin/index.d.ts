/**
 *
 * LoginSocialLinkedin
 *
 */
import React from 'react';
import { IResolveParams, objectType } from '../';
interface Props {
    state?: string;
    scope?: string;
    client_id: string;
    className?: string;
    redirect_uri: string;
    response_type?: string;
    children?: React.ReactNode;
    onReject: (reject: string | objectType) => void;
    onResolve: ({ provider, data }: IResolveParams) => void;
}
export declare const LoginSocialLinkedin: React.MemoExoticComponent<({ state, scope, client_id, className, redirect_uri, response_type, children, onResolve }: Props) => JSX.Element>;
export default LoginSocialLinkedin;
