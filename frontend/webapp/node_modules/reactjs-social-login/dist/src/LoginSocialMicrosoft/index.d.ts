/**
 *
 * LoginSocialMicrosoft
 *
 */
import React from 'react';
import { IResolveParams, objectType } from '../';
interface Props {
    scope?: string;
    state?: string;
    client_id: string;
    className?: string;
    redirect_uri: string;
    response_type?: string;
    response_mode?: string;
    code_challenge?: string;
    children?: React.ReactNode;
    onReject: (reject: string | objectType) => void;
    code_challenge_method?: 'plain' | 's256'[];
    onResolve: ({ provider, data }: IResolveParams) => void;
    tenant?: 'common' | 'organizations' | 'consumers';
    prompt?: 'login' | 'none' | 'consent' | 'select_account';
}
export declare const LoginSocialMicrosoft: React.MemoExoticComponent<({ tenant, state, client_id, className, redirect_uri, scope, response_type, response_mode, children, code_challenge, code_challenge_method, prompt, onReject, onResolve }: Props) => JSX.Element>;
export default LoginSocialMicrosoft;
