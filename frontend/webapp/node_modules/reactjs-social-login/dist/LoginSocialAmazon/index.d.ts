/**
 *
 * LoginSocialAmazon
 *
 */
import React from 'react';
import { IResolveParams, objectType } from '../';
interface Props {
    scope?: string;
    state?: string;
    client_id: string;
    className?: string;
    redirect_uri?: string;
    response_type?: string;
    scope_data?: objectType;
    children?: React.ReactNode;
    onReject: (reject: string | objectType) => void;
    onResolve: ({ provider, data }: IResolveParams) => void;
}
export declare const LoginSocialAmazon: React.MemoExoticComponent<({ state, client_id, className, redirect_uri, scope, scope_data, response_type, children, onReject, onResolve }: Props) => JSX.Element>;
export default LoginSocialAmazon;
