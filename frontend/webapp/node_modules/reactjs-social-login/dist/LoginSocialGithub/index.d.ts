/**
 *
 * LoginSocialGithub
 *
 */
import React from 'react';
import { IResolveParams, objectType } from 'types';
interface Props {
    state?: string;
    scope?: string;
    client_id: string;
    className?: string;
    redirect_uri: string;
    allow_signup?: boolean;
    children?: React.ReactNode;
    onReject: (reject: string | objectType) => void;
    onResolve: ({ provider, data }: IResolveParams) => void;
}
export declare const LoginSocialGithub: React.MemoExoticComponent<({ state, scope, client_id, className, redirect_uri, allow_signup, children, onResolve }: Props) => JSX.Element>;
export default LoginSocialGithub;
